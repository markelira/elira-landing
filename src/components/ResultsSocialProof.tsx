'use client'

import { SocialProof } from '@/components/landing/SocialProof'

/**
 * ResultsSocialProof component
 * Displays social proof with results and testimonials
 */
export function ResultsSocialProof() {
  return <SocialProof />
}
