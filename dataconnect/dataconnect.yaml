specVersion: "v1"
serviceId: "elira-landing"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "elira-landing-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
